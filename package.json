{
  "name": "homework-radar-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Pulls Google Classroom assignment emails from Gmail and writes homework-radar/assignments.json",
  "type": "commonjs",
  "scripts": {
    "fetch": "node scripts/fetch-assignments.js"
  },
  "dependencies": {
    "@google-cloud/local-auth": "^3.0.1",
    "google-auth-library": "^9.14.1",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
